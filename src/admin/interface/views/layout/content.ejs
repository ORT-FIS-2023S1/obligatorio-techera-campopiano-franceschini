<!-- content.ejs -->
<div class="col-md-9">
  <% if (view === 'dashboard') { %>
  <!-- Contenido del dashboard -->
  <%- include('../addProductCarousel') %>
  <!-- Aquí puedes incluir el contenido específico del dashboard -->
  <% } 
  else if (view === 'addDishe') { %> <%- include('../addDishe') %> <% }
  else if (view === 'addOrder') { %> <%- include('../addOrder') %> <% } 
  else if (view === 'addGroup') { %> <%- include('../addGroup') %> <% } 
  else if (view === 'editGroup') { %> <%- include('../editGroup') %> <% } %>
</div>
